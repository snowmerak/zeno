/**
 * @zeno/http-client — unit / smoke tests (placeholder)
 *
 * Per SKILL.md §5 "Testing Strategy", the authoritative integration tests
 * that use a real @zeno/http server live in integration_test.ts.
 *
 * This file is intentionally minimal. It is reserved for any future
 * pure (non-network) unit tests of internal helpers if they are
 * ever extracted. Currently no such exported helpers exist.
 */

// The real coverage (interceptors, HttpError, TimeoutError, query params,
// JSON body, default headers, timeout behavior, etc.) is exercised
// against a live server in integration_test.ts.

// No imports needed while this file remains a placeholder.
